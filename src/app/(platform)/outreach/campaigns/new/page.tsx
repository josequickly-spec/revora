import PageHeader from"@/components/app-shell/PageHeader";import{NewCampaignView}from"@/components/outreach/OutreachViews";export default async function Page({searchParams}:{searchParams:Promise<{businessId?:string;proposalId?:string}>}){const q=await searchParams;return <><PageHeader eyebrow="Outreach" title="Create campaign draft" description="Choose reviewed inputs. Nothing is approved, scheduled or sent automatically." actions={[{label:"All campaigns",href:"/outreach",tone:"secondary"}]}/><NewCampaignView businessId={q.businessId?Number(q.businessId):undefined} proposalId={q.proposalId}/></>}
