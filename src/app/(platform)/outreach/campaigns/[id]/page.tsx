import{notFound}from"next/navigation";import PageHeader from"@/components/app-shell/PageHeader";import{CampaignDetailView}from"@/components/outreach/OutreachViews";import{getCampaign}from"@/lib/outreach/store";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;if(!await getCampaign(id))notFound();return <><PageHeader eyebrow="Outreach" title="Campaign review" description="Review, approve and schedule are distinct explicit actions." actions={[{label:"All campaigns",href:"/outreach",tone:"secondary"}]}/><CampaignDetailView id={id}/></>}
