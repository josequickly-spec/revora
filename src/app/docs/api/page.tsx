import type { Metadata } from "next";
import PageHeader from "@/components/app-shell/PageHeader";
export const metadata:Metadata={title:"Enterprise API"};
export default function ApiDocsPage(){return <><PageHeader eyebrow="Developer platform" title="Enterprise REST API" description="OpenAPI 3.1 documentation for tenant-scoped CRM, security, billing and workflow endpoints." actions={[{label:"Open OpenAPI JSON",href:"/api/openapi"}]}/><section className="rounded-3xl border border-white/[.08] bg-white/[.03] p-6"><h2 className="text-xl font-bold text-white">Authentication</h2><p className="mt-3 text-sm leading-6 text-slate-400">Use a short-lived JWT or an API key in the Bearer authorization header. API keys are shown only once and remain restricted to the creator&apos;s permissions.</p><h2 className="mt-8 text-xl font-bold text-white">Safety boundary</h2><p className="mt-3 text-sm leading-6 text-slate-400">CRM reads events from upstream modules. Only an explicit stage command can change an opportunity stage.</p></section></>}
