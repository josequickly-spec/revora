import {NextResponse} from "next/server";import {enterpriseFailure} from "@/lib/enterprise/http";import {requirePermission} from "@/lib/enterprise/store";import {pool} from "@/lib/postgres";
export async function GET(request:Request){try{const context=await requirePermission(request,"workflow.read");const result=await pool.query(`SELECT id,alert_key AS "alertKey",severity,status,title,safe_summary AS "summary",evidence,first_seen_at AS "firstSeenAt",last_seen_at AS "lastSeenAt" FROM observability_alerts WHERE organization_id=$1 OR organization_id IS NULL ORDER BY status,severity,last_seen_at DESC LIMIT 200`,[context.organizationId]);return NextResponse.json({alerts:result.rows});}catch(error){return enterpriseFailure(error);}}
