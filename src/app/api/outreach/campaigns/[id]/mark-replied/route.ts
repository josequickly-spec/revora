import{NextResponse}from"next/server";import{markRecipientReplied}from"@/lib/outreach/store";export async function POST(r:Request,{params}:{params:Promise<{id:string}>}){try{const[{id},b]=await Promise.all([params,r.json()]);await markRecipientReplied(id,b.recipientId);return NextResponse.json({replied:true,source:"manual"})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Reply update failed."},{status:409})}}
