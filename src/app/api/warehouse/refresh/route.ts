import {NextResponse} from "next/server";import {enterpriseFailure} from "@/lib/enterprise/http";import {requirePermission} from "@/lib/enterprise/store";import {refreshWarehouse} from "@/lib/warehouse/snapshots/refresh";import {generateForecasts} from "@/lib/forecast/store";import {cacheDeletePattern} from "@/lib/cache";import {traced} from "@/lib/observability/tracing";
export async function POST(request:Request){try{const context=await requirePermission(request,"workflow.manage"),refresh=await traced("warehouse.refresh",()=>refreshWarehouse(context.organizationId)),forecasts=await generateForecasts(context.organizationId);await cacheDeletePattern(`executive:${context.organizationId}:`);return NextResponse.json({refresh,forecasts},{status:202});}catch(error){return enterpriseFailure(error);}}
