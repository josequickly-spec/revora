# Local Compose secrets

Create `postgres_password.txt`, `grafana_password.txt` and `metrics_token.txt` in this directory before running Compose. Use random values of at least 32 characters. These files are ignored by Git and must never be committed.
