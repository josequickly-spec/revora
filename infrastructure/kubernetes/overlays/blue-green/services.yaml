apiVersion: v1
kind: Service
metadata:
  name: revora-blue
spec:
  selector: {app: revora, slot: blue}
  ports: [{port: 80, targetPort: http}]
---
apiVersion: v1
kind: Service
metadata:
  name: revora-green
spec:
  selector: {app: revora, slot: green}
  ports: [{port: 80, targetPort: http}]
